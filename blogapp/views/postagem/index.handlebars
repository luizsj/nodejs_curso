<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <p>
            <small>Publicado em {{data}}</small>
        </p>
        <hr>
        {{postagem.conteudo}}
    </div>
</div>
